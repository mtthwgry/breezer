<p>filter by user | <%= link_to "deselect all", '#', id: 'deselect' %></p>
<ul class="user-list">
  <% @users.each do |user| %>
    <li class="user-list-item"><%= link_to user.name, '#', data: {'user-id': user.id} %></li>
  <% end %>
</ul>